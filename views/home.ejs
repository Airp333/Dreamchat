<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=0.8">
    <title>Chat</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Ubuntu:ital,wght@0,300;0,400;0,500;0,700;1,300;1,400;1,500;1,700&family=Vazirmatn:wght@100..900&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@24,400,0,0&icon_names=delete,attach_file,edit,send,logout,palette" />
<style>
    body {
        background-image: linear-gradient(to top, #09203f 0%, #537895 100%);
        font-family: "Ubuntu", "Vazirmatn", sans-serif;
        color: #dbdee1;
        margin: 20px;
        justify-content: center;
        align-items: center;
    }
    html {
      height: 100%;
   }
    input {
        font-family: "Ubuntu", sans-serif;
        width: 100%;
        background: #383a40;
        border: 1px solid #26282c;
        padding: 5px;
        color: #dbdee1;
        outline: none;
        transition: border-color 0.15s ease, box-shadow 0.15s ease;
    }
  input:focus{
    border-color: rgba(255, 255, 255, 0.35);
    box-shadow: 0 0 0 3px rgba(255, 255, 255, 0.35);
  }
    button {
        font-family: "Ubuntu", sans-serif;
        border: none;
        border-radius: 8px;
        color: #ffffff;
        padding: 10px;
        cursor: pointer;
        transition: transform 0.1s ease, box-shadow 0.1s ease, opacity 0.1s ease;
    }
    button:hover {
        box-shadow: 0 4px 10px rgba(0, 0, 0, 0.25);
        transform: translateY(-1px);
    }
    button:active {
        transform: translateY(0) scale(0.96);
    }
    button:disabled {
        opacity: 0.5;
        cursor: not-allowed;
        transform: none;
        box-shadow: none;
    }
    h2 {
        margin: 0;
        /* border-bottom: solid 1px rgba(255, 255, 255, 0.2); */
        padding: 16px 20px;
        /* margin-bottom: 15px; */
        text-shadow: 2px 2px 5px rgba(0, 0, 0, 0.1);
        padding-right: 5px;
    }
    p {
        text-align: center;
        text-shadow: 2px 2px 5px rgba(0, 0, 0, 0.1);
    }
    .glass-container {
        background: rgba(0, 0, 0, 0.1);
        color: white;
        backdrop-filter: blur(10px);
        border-radius: 10px;
        border: solid 1px rgba(255, 255, 255, 0.4);
        text-shadow: 2px 2px 5px rgba(100, 100, 100, 0.6);
    }
    #main {
        width: 100%;
    }
    input::placeholder {
        color: white;
    }
    #form {
        display: flex;
    }
    #messages {
        max-height: 500px;
        overflow-y: auto;
        scroll-behavior: smooth;
        padding: 12px 0;
    }
    #messages::-webkit-scrollbar {
        width: 8px;
    }
    #messages::-webkit-scrollbar-track {
        background: rgba(0, 0, 0, 0.1);
        backdrop-filter: blur(10px);
        border-radius: 10px;
    }
    #messages::-webkit-scrollbar-thumb {
        background: rgba(125, 125, 125, 0.5);
        border-radius: 4px;
    }
    .message-container {
        display: grid;
        grid-template-columns: auto 1fr auto auto auto;
        gap: 10px;
        align-items: center;
        padding: 6px 12px;
        margin: 2px 8px;
        border-radius: 6px;
        font-weight: 400;
        font-size: 15px;
        color: white;
        transition: background 0.1s ease;
    }
    .message-container:hover {
        background: rgba(255, 255, 255, 0.1);
    }
    .message-container .username {
        grid-column: 1;
        grid-row: 1;
    }
    .message-container .msg-text {
        grid-column: 2;
        grid-row: 1;
    }
    .message-container .msg-time {
        grid-column: 5;
        justify-self: end;
        font-size: 12px;
        opacity: 0.5;
        margin-left: 8px;
        grid-row: 1;
    }
    .message-container .delete-btn {
        align-self: center
        grid-column: 3;
        justify-self: end;
        background: rgba(255, 255, 255, 0.1);
        color: white;
        backdrop-filter: blur(10px);
        border-radius: 15px;
        border: solid 1px rgba(255, 255, 255, 0.4);
        box-shadow: 2px 2px 5px rgba(255, 255, 255, 0.1);
        text-shadow: 2px 2px 5px rgba(100, 100, 100, 0.5);
        padding: 5px;
        width: 36px;
        height: 36px;
        display: flex;
        align-items: center;
        justify-content: center;
        cursor: pointer;
    }
    .message-container .edit-btn {
        align-self: center
        grid-column: 4;
        justify-self: end;
        background: rgba(255, 255, 255, 0.1);
        color: white;
        backdrop-filter: blur(10px);
        border-radius: 15px;
        border: solid 1px rgba(255, 255, 255, 0.4);
        box-shadow: 2px 2px 5px rgba(255, 255, 255, 0.1);
        text-shadow: 2px 2px 5px rgba(100, 100, 100, 0.5);
        padding: 5px;
        width: 36px;
        height: 36px;
        display: flex;
        align-items: center;
        justify-content: center;
        cursor: pointer;
    }
    .message-container .delete-btn .material-symbols-outlined,
    .message-container .edit-btn .material-symbols-outlined {
        font-size: 18px;
    }
    #title-container{
       display: flex;
    }
    .logout-btn{
        background: rgba(255, 255, 255, 0.1);
        color: white;
        backdrop-filter: blur(10px);
        border-radius: 15px;
        border: solid 1px rgba(255, 255, 255, 0.4);
        box-shadow: 2px 2px 5px rgba(255, 255, 255, 0.1);
        text-shadow: 2px 2px 5px rgba(100, 100, 100, 0.5);
        padding: 5px;
        width: 36px;
        height: 36px;
        display: flex;
        cursor: pointer;
        align-items: center;
        justify-content: center;
        align-self: center;
    }
    .logout-btn .material-symbols-outlined {
        font-size: 18px;
    }
    .logout-form{
        align-items: center;
        justify-content: center;
        align-self: center;
    }
    .theme-btn{
        position: fixed;
        bottom: 15px;
        left: 15px;
    }
    .theme-btn{
        position: fixed;
        bottom: 15px;
        left: 15px;
        background: rgba(0, 0, 0, 0.1);
        color: white;
        backdrop-filter: blur(10px);
        border-radius: 15px;
        border: solid 1px rgba(255, 255, 255, 0.4);
        text-shadow: 2px 2px 5px rgba(100, 100, 100, 0.5);
        padding: 5px;
        width: 55px;
        height: 55px;
        display: flex;
        cursor: pointer;
        align-items: center;
        justify-content: center;
        align-self: center;
    }
    .theme-btn .material-symbols-outlined {
        font-size: 27px;
    }
    </style>
</head>
<body>
    <div id="main" class="glass-container">
        <div id="title-container" class="glass-container"><h2>Dreamchat v1.3</h2><form class="logout-form" action="/logout" method="GET"><button class="logout-btn"><span class="material-symbols-outlined">logout</span></button></form></div>
        <div id="messages">
            <% coloredPayam.forEach((payam) => { %>
<div class="message-container" data-id="<%= payam.id %>" data-username="<%= payam.username %>" data-color="<%= payam.color %>">
    <span class="username" style="color: <%= payam.color %>"><%= payam.username %></span>
    <span class="msg-text"><%= payam.text %></span>
    <span class="msg-time"><%= new Date(payam.createdAt).toLocaleTimeString([], {hour: '2-digit', minute:'2-digit'}) %></span>
    <% if (payam.username === username) { %>
        <button class="delete-btn"><span class="material-symbols-outlined">delete</span></button>
        <button class="edit-btn"><span class="material-symbols-outlined">edit</span></button>
    <% } %>
</div>
            <% }) %>
        </div>
        <form id="form">
            <input id="input" type="text" placeholder="Type a message..." class="glass-container">
            <button type="submit" class="glass-container">
                <span class="material-symbols-outlined">send</span>
            </button>
        </form>
    </div>

    <button class="theme-btn" ><span class="material-symbols-outlined">palette</span></button>
    <script src="/socket.io/socket.io.js"></script>
    <script>
        const socket = io();
        const form = document.getElementById('form');
        const input = document.getElementById('input');
        const messages = document.getElementById('messages');
        const username = "<%= username %>";

        function usernameToColor(username) {
            let hash = 0;
            for (let i = 0; i < username.length; i++) {
                hash = username.charCodeAt(i) + ((hash << 5) - hash);
            }
            const hue = Math.abs(hash) % 360;
            return `hsl(${hue}, 70%, 65%)`;
        }

        form.addEventListener('submit', (e) => {
            e.preventDefault();
            const text = input.value.trim();
            if (!text) return;
            socket.emit('chatMessage', text);
            input.value = '';
            input.focus();
        });

socket.on('chat message', (data) => {
    const div = document.createElement('div');
    div.className = 'message-container';
    div.dataset.id = data.id;
    div.dataset.username = data.username;
    div.dataset.color = usernameToColor(data.username);

    const usernameSpan = document.createElement('span');
    usernameSpan.className = 'username';
    usernameSpan.style.color = usernameToColor(data.username);
    usernameSpan.textContent = data.username;
    div.appendChild(usernameSpan);

    const textSpan = document.createElement('span');
    textSpan.className = 'msg-text';
    textSpan.textContent = data.text;
    div.appendChild(textSpan);

    const timeSpan = document.createElement('span');
    timeSpan.className = 'msg-time';
    timeSpan.textContent = new Date(data.createdAt).toLocaleTimeString([], {hour: '2-digit', minute:'2-digit'});

    div.appendChild(timeSpan);

    if (data.username === username) {
        const deleteBtn = document.createElement('button');
        deleteBtn.className = 'delete-btn';
        const deleteIcon = document.createElement('span');
        deleteIcon.className = 'material-symbols-outlined';
        deleteIcon.textContent = 'delete';
        deleteBtn.appendChild(deleteIcon);
        div.appendChild(deleteBtn);

        const editBtn = document.createElement('button');
        editBtn.className = 'edit-btn';
        const editIcon = document.createElement('span');
        editIcon.className = 'material-symbols-outlined';
        editIcon.textContent = 'edit';
        editBtn.appendChild(editIcon);
        div.appendChild(editBtn);
    }

    messages.appendChild(div);
    messages.scrollTop = messages.scrollHeight;
});

        socket.on('message deleted', (id) => {
            const div = document.querySelector(`[data-id="${id}"]`);
            if (div) div.remove();
        });

        socket.on('message edited', ({ id, text }) => {
            const div = document.querySelector(`[data-id="${id}"]`);
            if (!div) return;
            const textSpan = div.querySelector('.msg-text');
            if (textSpan) {
                textSpan.textContent = text;
            }
        });

        messages.addEventListener('click', (e) => {
            if (e.target.closest('.delete-btn')) {
                const button = e.target.closest('.delete-btn');
                const div = button.closest('div');
                const id = div.dataset.id;
                socket.emit('deleteMessage', id);
                return;
            }

            if (e.target.closest('.edit-btn')) {
                const button = e.target.closest('.edit-btn');
                const div = button.closest('div');
                const id = div.dataset.id;
                const textSpan = div.querySelector('.msg-text');
                const currentText = textSpan ? textSpan.textContent : '';
                const newText = prompt('Edit your message:', currentText);
                if (newText !== null && newText.trim() !== '') {
                    socket.emit('editMessage', { id, text: newText.trim() });
                }
                return;
            }
        });
    </script>
    <script>
        const themeBtn = document.querySelector('.theme-btn')
        const body = document.querySelector('body')

        function getRandomElement(arr) {
            const randomIndex = Math.floor(Math.random() * arr.length);
            return arr[randomIndex];
        }


        themeBtn.addEventListener('click', (e) => {
           const grads = [
              "linear-gradient(to top, #09203f 0%, #537895 100%)",
              "linear-gradient(to right, #f83600 0%, #f9d423 100%)",
              "linear-gradient(to top, #c79081 0%, #dfa579 100%)",
              "linear-gradient(to top, #0ba360 0%, #3cba92 100%)",
              "linear-gradient(to right, #ff8177 0%, #ff867a 0%, #ff8c7f 21%, #f99185 52%, #cf556c 78%, #b12a5b 100%)",
              "linear-gradient(to top, #a18cd1 0%, #fbc2eb 100%)",
              "linear-gradient(-20deg, #616161 0%, #9bc5c3 100%)",
              "linear-gradient(to right, #434343 0%, black 100%)"
           ]

          body.style.backgroundImage = getRandomElement(grads);
        })
    </script>
</body>
</html>
